{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "Create api gateway to execute all microservices.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "harshit",
  "license": "ISC",
  "dependencies": {
    "express-rate-limit": "^6.7.0",
    "fast-gateway": "^3.4.5",
    "request-ip": "^3.3.0"
  }
}
